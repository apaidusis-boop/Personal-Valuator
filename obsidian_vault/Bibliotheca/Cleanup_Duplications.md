---
type: cleanup_report
date: 2026-04-27
phase: midnight_work
refactored: 4
documented: 11
loc_saved: 49
tags: [bibliotheca, cleanup, refactor]
---

# 🧹 Cleanup: Duplicated Logic — 2026-04-27 (overnight)

## Summary
- **Duplications found**: 11 (5 high-value, 6 low-value/risky)
- **Refactored**: 4 (all routed through new `agents/_common.py`)
- **Documented for review**: 7
- **LoC saved at callsites**: ~49 (net of import lines added)
- **New shared module**: `agents/_common.py` (~95 lines) — home for cross-module utilities

## Refactored

| Function | Before (locations) | After (canonical) | Callers updated |
|---|---|---|---|
| `slugify` (ASCII slug) | `scripts/obsidian_bridge.py::_slugify`, `scripts/vault_clean_video_names.py::_slugify` (identical impls) | `agents/_common.py::slugify` | both files now `from agents._common import slugify as _slugify`; removed local impls + `unicodedata` import |
| `read_vault_thesis` (find ## Thesis) | `agents/variant_perception.py::_vault_thesis` (most complete, dossier-aware), `agents/synthetic_ic.py::_vault_thesis` (incomplete — missed dossier files) | `agents/_common.py::read_vault_thesis` | `synthetic_ic` + `variant_perception` are thin wrappers. **Bonus**: synthetic_ic now reads dossier theses (Phase G+) it was previously missing. |

All 4 refactored modules pass `python -c "import …"` smoke tests and `read_vault_thesis('ITSA4')` returns the expected 800-char body.

## Documented (not refactored)

| # | Pattern | Locations | Reason |
|---|---|---|---|
| 1 | `_vault_thesis` in library/ | `library/earnings_prep.py`, `scripts/overnight/ab_qwen3_vs_14b.py` | `library/` off-limits; one-shot script |
| 2 | `_slugify` in library/ | `library/ingest.py`, `library/clippings_ingest.py` | `library/` off-limits |
| 3 | `_chunk` paragraph chunker | `library/ingest.py`, `library/clippings_ingest.py` (near-identical) | `library/` off-limits |
| 4 | `_parse_frontmatter` | 3 different signatures across `memory_cleanup`, `_captains_log`, `clippings_ingest` | divergent APIs (path vs text vs tuple) |
| 5 | Ollama call constants | `_llm.py` canonical, but 8+ files bypass it (synthetic_ic, thesis_synthesizer, variant_perception, extract_insights, earnings_prep, _pdf_extract, 3 overnight scripts) | each callsite uses different prompts/JSON/temp/timeouts; need extended `_llm.py` API |
| 6 | `DB_BR`/`DB_US` constants | 70+ files | tiny copy-paste; cosmetic refactor |
| 7 | `_load_universe` | `daily_update.py`, `daily_update_us.py`, `bibliotheca_autofix.py` | three distinct return types |
| 8 | `_section(label)` banner | `daily_update.py`, `daily_update_us.py` | 3-line helper, near-zero ROI |
| 9-11 | `_fmt_pct`, `_fmt_num`, `_fmt_money` | `dossier.py`, `compare_tickers.py`, `drip_projection.py` | divergent signatures (intentional formatting differences) |

## Suggestions for next session

1. **Library unlock** — migrate `_chunk`, `_slugify`, `_file_hash` from `library/` to shared module. Est. ~40 LoC.
2. **Ollama wrapper expansion** — extend `agents/_llm.py` with low-level `ollama_call(prompt, *, json_mode, format, temperature, timeout, model)`. Will save ~80-100 LoC.
3. **DB path module** — opportunistic `analytics.dbs` migration when files are otherwise touched.
4. **Wire `agents._common.section`** into `daily_update*.py` (trivial).
5. **Frontmatter parser unification** — design rich/lite API split with unit tests.

## Files modified
- `agents/_common.py` (new, ~95 LoC)
- `scripts/obsidian_bridge.py` (–9 LoC, +1 import)
- `scripts/vault_clean_video_names.py` (–9 LoC, +1 import; removed `unicodedata`)
- `agents/synthetic_ic.py` (–10 LoC, +8 LoC wrapper)
- `agents/variant_perception.py` (–38 LoC, +9 LoC wrapper)

---
*Auto-generated by midnight cleanup subagent #2 (2026-04-27).*
